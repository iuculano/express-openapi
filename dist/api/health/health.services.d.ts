/**
 * Checks the connectivity to Keycloak.
 *
 * @returns
 * A promise that resolves to `true` if Keycloak is reachable, otherwise
 * `false`.
 */
declare function checkKeycloak(): Promise<boolean>;
declare const _default: {
    checkKeycloak: typeof checkKeycloak;
};
export default _default;
//# sourceMappingURL=health.services.d.ts.map